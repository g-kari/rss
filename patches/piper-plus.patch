diff --git a/src/index.js b/src/index.js
index 2c91582..4921f15 100644
--- a/src/index.js
+++ b/src/index.js
@@ -947,6 +947,7 @@ export class PiperPlus {
           wasmAdapter = await RustWasmAdapter.create(JSON.stringify(this._config), {
             wasmUrl: options.wasmG2pUrl || "../../dist/rust-wasm/piper_plus_wasm.js",
             wasmLoader: options.wasmLoader,
+            zhDictBaseUrl: options.zhDictBaseUrl,
           });
           // Route all languages through WASM
           const wasmLangs = wasmAdapter.supportedLanguages;
@@ -1133,7 +1134,7 @@ export class PiperPlus {
           speakerEmbedding.length,
         ]);
         if (hasSpeakerEmbeddingMask) {
-          feeds.speaker_embedding_mask = new ort.Tensor("int64", new BigInt64Array([1n]), [1]);
+          feeds.speaker_embedding_mask = new ort.Tensor("int64", new BigInt64Array([1n]), [1, 1]);
         }
       } else {
         console.warn(
@@ -1141,7 +1142,7 @@ export class PiperPlus {
         );
         feeds.speaker_embedding = new ort.Tensor("float32", new Float32Array(192), [1, 192]);
         if (hasSpeakerEmbeddingMask) {
-          feeds.speaker_embedding_mask = new ort.Tensor("int64", new BigInt64Array([0n]), [1]);
+          feeds.speaker_embedding_mask = new ort.Tensor("int64", new BigInt64Array([0n]), [1, 1]);
         }
       }
     } else if (speakerEmbedding && speakerEmbedding.length > 0) {
@@ -1153,7 +1154,7 @@ export class PiperPlus {
         speakerEmbedding.length,
       ]);
       if (hasSpeakerEmbeddingMask) {
-        feeds.speaker_embedding_mask = new ort.Tensor("int64", new BigInt64Array([1n]), [1]);
+        feeds.speaker_embedding_mask = new ort.Tensor("int64", new BigInt64Array([1n]), [1, 1]);
       }
     }

diff --git a/src/phonemizer/rust-wasm-adapter.js b/src/phonemizer/rust-wasm-adapter.js
index 8b56a75..ab9f4ad 100644
--- a/src/phonemizer/rust-wasm-adapter.js
+++ b/src/phonemizer/rust-wasm-adapter.js
@@ -69,7 +69,11 @@ export class RustWasmAdapter {
       if (!url) {
         throw new Error("Either wasmUrl or wasmLoader must be provided");
       }
-      wasmModule = await import(url);
+      // PATCHED: Next.js 16 Turbopack cannot statically resolve a runtime URL and
+      // fails the production build with `Can't resolve <dynamic>`. Keep the
+      // upstream runtime import semantics while hiding it from static analysis.
+      const importFn = new Function("url", "return import(url)");
+      wasmModule = await importFn(url);
       await wasmModule.default(); // init() — load WASM binary
     }
     const wasm = new wasmModule.WasmPhonemizer(configJson);
